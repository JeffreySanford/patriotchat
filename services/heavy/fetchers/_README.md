# Fetchers (moved)

This directory previously held fetcher implementations. They have been moved into the parent directory (`services/heavy`) so they can share registry and proposal types.

The files here are kept as placeholders to avoid accidental re-adding conflicting code.
