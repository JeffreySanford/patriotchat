// WebSocket gateway disabled to resolve Socket.IO HTTP routing conflicts
// TODO: Re-enable on separate port with standalone Socket.IO server if needed

export class TelemetryGateway {
  // Stub class to prevent compilation errors
}
