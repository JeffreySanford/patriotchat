/**
 * API DTOs for frontend
 * Re-exported from shared library for convenient local imports
 * All types are defined in the shared library for consistency across frontend and backend
 */

export {
  InferenceModelsResponse,
  InferenceGenerateRequest,
  InferenceGenerateResponse,
  ApiError,
  ValidationError,
  ErrorDetails,
  ApiResponse,
} from '@patriotchat/shared';
